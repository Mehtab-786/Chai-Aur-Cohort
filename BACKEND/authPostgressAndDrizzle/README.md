build --> "tsc -p",
run continously --> "tsc-watch --onSuccess \"node dist/index\""
npx tsc --init  --> to generate tsconfig.json


docker ps -> to check all the runnning containers
docker images --> check images on machine 

docker compose up   -> to run machine in dcoker will stop at ctrl-c
docker compose up  -d -> to run machine in dcoker in the behind scenes
